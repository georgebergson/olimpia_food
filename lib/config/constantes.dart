import 'package:flutter/material.dart';

// Todas as Cores do Aplicativo
const corPrimaria = Color(0xFFdc3545);
const corSecundaria = Color(0xFF151710);

const coPrimariaGradiente = LinearGradient(
    begin: Alignment.topRight,
    end: Alignment.bottomLeft,
    colors: [Color(0xFFdc3545), Color(0xFFb50011)]);
// Final das Cores do Aplicativo

// Tamanho dos Widget

// Fim Tamanho dos Objetos
